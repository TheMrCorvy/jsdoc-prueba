//@ts-check

/**
 * an array of users' ages
 *
 * @type {Array}
 */
const age = [19, 20, 21];

/**
 * array of users scores
 *
 * @type {Array<Number>}
 */
const scores = [0, 199, 32, 23, 52];

/**
 * an array of numbers and strings
 *
 * @type {Array<number|string>}
 */

const numberAndString = ['hola', 32];
